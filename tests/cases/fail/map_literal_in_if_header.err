# Grammar 14.13. The offending literal is a map, so the help must not
# illustrate the fix with a struct literal.
code E0454
text map literals are not allowed in the header of if / while / for / match
text wrap it in parentheses
absent (User { name: "x" })
