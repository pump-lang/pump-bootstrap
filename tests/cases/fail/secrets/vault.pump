pub const PUBLIC_NAME: string = "vault"

const COMBINATION: int = 1234

pub struct Box {
    pub label: string
    contents: string
}

pub fn make(): Box {
    return Box { label: "b", contents: "gold" }
}

fn hidden(): int {
    return COMBINATION
}
